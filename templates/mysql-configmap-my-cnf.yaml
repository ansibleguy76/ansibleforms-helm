{{- if dig "enabled" true (.Values.mysql | default dict) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-my-cnf
  namespace: {{ .Release.Namespace }}
data:
  my.cnf: |
    [mysqld]
{{- end }}
